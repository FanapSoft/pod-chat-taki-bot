import { Meteor } from 'meteor/meteor';
import chatClients from '../../lib/talky/chatClients';

/*Meteor.publish('botClientStatus', function () {
  this.insert("botClientStatus", "bot_client_status", chatClients.botClientStatus);
  // signal ready
  this.ready();
  //return chatClients.botClientStatus
});*/
/*

Meteor.publish('adminClientStatus', function () {
  return chatClients.adminClientStatus
});
*/

